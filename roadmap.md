# Roadmap

- Min/max filters: tranforms that receives N analog inputs and returns min/max values;
- And/Or operators: transforms that receives N digital inputs and returns and/or operation result;
- Magnifier/curve transform for analogs
- AnalogToDigitalConverter: support for map multiple ranges for true signal